# Archive

This folder preserves older/extra project artifacts that are **not** part of the final submission entrypoints.

- `archive/reports/` — older memo versions + proposal
- `archive/notebooks/` — intermediate notebooks + scratch
- `archive/figures/memo/` — figure exports used by the older memo version

The grader-facing entrypoints are in `SUBMISSION.md`, `reports/`, and `notebooks/dst_crime_ca_az_refined.ipynb`.
